diff --git a/src/corelib/kernel/qcore_unix_p.h b/src/corelib/kernel/qcore_unix_p.h
index f20561fc839..828c7ddb8e0 100644
--- a/src/corelib/kernel/qcore_unix_p.h
+++ b/src/corelib/kernel/qcore_unix_p.h
@@ -253,8 +253,8 @@ static inline int qt_safe_dup2(int oldfd, int newfd, int flags = FD_CLOEXEC)
     Q_ASSERT(flags == FD_CLOEXEC || flags == 0);
 
     int ret;
-#if QT_CONFIG(dup3)
-    // use dup3
+#if QT_CONFIG(dup3) && (!defined(Q_OS_DARWIN) || __MAC_OS_X_VERSION_MIN_REQUIRED >= 270000)
+    // use dup3 (Darwin added it in macOS 27, weak import is null on older systems)
     QT_EINTR_LOOP(ret, ::dup3(oldfd, newfd, flags ? O_CLOEXEC : 0));
     return ret;
 #else
