diff --git a/src/network/access/qhttpnetworkconnection.cpp b/src/network/access/qhttpnetworkconnection.cpp
index 20b8d446bac..c29f5f3852f 100644
--- a/src/network/access/qhttpnetworkconnection.cpp
+++ b/src/network/access/qhttpnetworkconnection.cpp
@@ -436,8 +436,11 @@ bool QHttpNetworkConnectionPrivate::handleAuthenticateChallenge(QAbstractSocket
     //create the response header to be used with QAuthenticatorPrivate.
     QList<QPair<QByteArray, QByteArray> > fields = reply->header();
 
+    // NTLM/Negotiate proxy authentication is only honored for HTTP proxies that
+    // come from the system configuration (see QNetworkProxy::isNtlmAuthenticationAllowed).
+    const bool allowUnsafeProxyAuth = isProxy && networkProxy.isNtlmAuthenticationAllowed();
     //find out the type of authentication protocol requested.
-    QAuthenticatorPrivate::Method authMethod = reply->d_func()->authenticationMethod(isProxy);
+    QAuthenticatorPrivate::Method authMethod = reply->d_func()->authenticationMethod(isProxy, allowUnsafeProxyAuth);
     if (authMethod != QAuthenticatorPrivate::None) {
         int i = indexOf(socket);
         //Use a single authenticator for all domains. ### change later to use domain/realm
@@ -453,7 +456,7 @@ bool QHttpNetworkConnectionPrivate::handleAuthenticateChallenge(QAbstractSocket
         if (auth->isNull())
             auth->detach();
         QAuthenticatorPrivate *priv = QAuthenticatorPrivate::getPrivate(*auth);
-        priv->parseHttpResponse(fields, isProxy, reply->url().host());
+        priv->parseHttpResponse(fields, isProxy, reply->url().host(), allowUnsafeProxyAuth);
         // Update method in case it changed
         if (priv->method == QAuthenticatorPrivate::None)
             return false;
@@ -637,16 +640,22 @@ void QHttpNetworkConnectionPrivate::createAuthorization(QAbstractSocket *socket,
         if (priv && priv->method != QAuthenticatorPrivate::None) {
             const bool ntlmNego = channels[i].proxyAuthMethod == QAuthenticatorPrivate::Ntlm
                     || channels[i].proxyAuthMethod == QAuthenticatorPrivate::Negotiate;
-            const bool proxyAuthNeeded = channels[i].lastStatus == 407;
-            const bool ntlmNegoOk = ntlmNego && proxyAuthNeeded
-                    && (priv->phase != QAuthenticatorPrivate::Done
-                        || !channels[i].proxyCredentialsSent);
-            const bool otherOk = !ntlmNego;
-            if (ntlmNegoOk || otherOk) {
-                QByteArray response = priv->calculateResponse(
-                        request.methodName(), request.uri(false), networkProxy.hostName());
-                request.setHeaderField("Proxy-Authorization", response);
-                channels[i].proxyCredentialsSent = true;
+            if (ntlmNego && !networkProxy.isNtlmAuthenticationAllowed()) {
+                channels[i].proxyAuthMethod = QAuthenticatorPrivate::None;
+                channels[i].proxyAuthenticator = QAuthenticator();
+                channels[i].proxyCredentialsSent = false;
+            } else {
+                const bool proxyAuthNeeded = channels[i].lastStatus == 407;
+                const bool ntlmNegoOk = ntlmNego && proxyAuthNeeded
+                        && (priv->phase != QAuthenticatorPrivate::Done
+                            || !channels[i].proxyCredentialsSent);
+                const bool otherOk = !ntlmNego;
+                if (ntlmNegoOk || otherOk) {
+                    QByteArray response = priv->calculateResponse(
+                            request.methodName(), request.uri(false), networkProxy.hostName());
+                    request.setHeaderField("Proxy-Authorization", response);
+                    channels[i].proxyCredentialsSent = true;
+                }
             }
         }
     }
diff --git a/src/network/access/qhttpnetworkreply.cpp b/src/network/access/qhttpnetworkreply.cpp
index 2024c0fefb2..53b42782163 100644
--- a/src/network/access/qhttpnetworkreply.cpp
+++ b/src/network/access/qhttpnetworkreply.cpp
@@ -416,6 +416,26 @@ void QHttpNetworkReplyPrivate::removeAutoDecompressHeader()
     }
 }
 
+static bool qStartsWithAuthenticationScheme(const QByteArray &value, const char *scheme)
+{
+    const QByteArray trimmed = value.trimmed();
+    const int schemeSize = int(qstrlen(scheme));
+    if (trimmed.size() < schemeSize)
+        return false;
+    if (trimmed.left(schemeSize).compare(scheme, Qt::CaseInsensitive) != 0)
+        return false;
+    if (trimmed.size() == schemeSize)
+        return true;
+    const char separator = trimmed.at(schemeSize);
+    return separator == ' ' || separator == '\t' || separator == ',';
+}
+
+static bool qIsUnsafeProxyAuthenticationScheme(const QByteArray &value)
+{
+    return qStartsWithAuthenticationScheme(value, "ntlm")
+        || qStartsWithAuthenticationScheme(value, "negotiate");
+}
+
 bool QHttpNetworkReplyPrivate::findChallenge(bool forProxy, QByteArray &challenge) const
 {
     challenge.clear();
@@ -432,25 +452,27 @@ bool QHttpNetworkReplyPrivate::findChallenge(bool forProxy, QByteArray &challeng
     return !challenge.isEmpty();
 }
 
-QAuthenticatorPrivate::Method QHttpNetworkReplyPrivate::authenticationMethod(bool isProxy) const
+QAuthenticatorPrivate::Method QHttpNetworkReplyPrivate::authenticationMethod(bool isProxy, bool allowUnsafeProxyMethods) const
 {
     // The logic is same as the one used in void QAuthenticatorPrivate::parseHttpResponse()
     QAuthenticatorPrivate::Method method = QAuthenticatorPrivate::None;
     QByteArray header = isProxy ? "proxy-authenticate" : "www-authenticate";
     QList<QByteArray> challenges = headerFieldValues(header);
     for (int i = 0; i<challenges.size(); i++) {
-        QByteArray line = challenges.at(i).trimmed().toLower();
+        const QByteArray line = challenges.at(i).trimmed();
+        if (isProxy && !allowUnsafeProxyMethods && qIsUnsafeProxyAuthenticationScheme(line))
+            continue;
         if (method < QAuthenticatorPrivate::Basic
-            && line.startsWith("basic")) {
+            && qStartsWithAuthenticationScheme(line, "basic")) {
             method = QAuthenticatorPrivate::Basic;
         } else if (method < QAuthenticatorPrivate::Ntlm
-            && line.startsWith("ntlm")) {
+            && qStartsWithAuthenticationScheme(line, "ntlm")) {
             method = QAuthenticatorPrivate::Ntlm;
         } else if (method < QAuthenticatorPrivate::DigestMd5
-            && line.startsWith("digest")) {
+            && qStartsWithAuthenticationScheme(line, "digest")) {
             method = QAuthenticatorPrivate::DigestMd5;
         } else if (method < QAuthenticatorPrivate::Negotiate
-            && line.startsWith("negotiate")) {
+            && qStartsWithAuthenticationScheme(line, "negotiate")) {
             method = QAuthenticatorPrivate::Negotiate;
         }
     }
diff --git a/src/network/access/qhttpnetworkreply_p.h b/src/network/access/qhttpnetworkreply_p.h
index 9c03a403e17..e28dbe52cde 100644
--- a/src/network/access/qhttpnetworkreply_p.h
+++ b/src/network/access/qhttpnetworkreply_p.h
@@ -202,7 +202,7 @@ public:
     qint64 readBodyVeryFast(QAbstractSocket *socket, char *b);
     qint64 readBodyFast(QAbstractSocket *socket, QByteDataBuffer *rb);
     bool findChallenge(bool forProxy, QByteArray &challenge) const;
-    QAuthenticatorPrivate::Method authenticationMethod(bool isProxy) const;
+    QAuthenticatorPrivate::Method authenticationMethod(bool isProxy, bool allowUnsafeProxyMethods = false) const;
     void clear();
     void clearHttpLayerInformation();
 
diff --git a/src/network/kernel/qauthenticator.cpp b/src/network/kernel/qauthenticator.cpp
index 93b323f0bd9..c93c5df4515 100644
--- a/src/network/kernel/qauthenticator.cpp
+++ b/src/network/kernel/qauthenticator.cpp
@@ -440,7 +440,28 @@ static bool verifyDigestMD5(const QByteArray &value)
     return true; // assume it's ok if algorithm is not specified
 }
 
-void QAuthenticatorPrivate::parseHttpResponse(const QList<QPair<QByteArray, QByteArray> > &values, bool isProxy, const QString &host)
+static bool qStartsWithAuthenticationScheme(const QByteArray &value, const char *scheme)
+{
+    const QByteArray trimmed = value.trimmed();
+    const int schemeSize = int(qstrlen(scheme));
+    if (trimmed.size() < schemeSize)
+        return false;
+    if (trimmed.left(schemeSize).compare(scheme, Qt::CaseInsensitive) != 0)
+        return false;
+    if (trimmed.size() == schemeSize)
+        return true;
+    const char separator = trimmed.at(schemeSize);
+    return separator == ' ' || separator == '\t' || separator == ',';
+}
+
+static bool qIsUnsafeProxyAuthenticationScheme(const QByteArray &value)
+{
+    return qStartsWithAuthenticationScheme(value, "ntlm")
+        || qStartsWithAuthenticationScheme(value, "negotiate");
+}
+
+void QAuthenticatorPrivate::parseHttpResponse(const QList<QPair<QByteArray, QByteArray> > &values, bool isProxy, const QString &host,
+                                              bool allowUnsafeProxyMethods)
 {
 #if !QT_CONFIG(gssapi)
     Q_UNUSED(host);
@@ -459,26 +480,31 @@ void QAuthenticatorPrivate::parseHttpResponse(const QList<QPair<QByteArray, QByt
     */
 
     QByteArray headerVal;
+    bool skippedUnsafeProxyAuthenticationScheme = false;
     for (int i = 0; i < values.size(); ++i) {
         const QPair<QByteArray, QByteArray> &current = values.at(i);
         if (current.first.compare(search, Qt::CaseInsensitive) != 0)
             continue;
-        QByteArray str = current.second.toLower();
-        if (method < Basic && str.startsWith("basic")) {
+        const QByteArray value = current.second.trimmed();
+        if (isProxy && !allowUnsafeProxyMethods && qIsUnsafeProxyAuthenticationScheme(value)) {
+            skippedUnsafeProxyAuthenticationScheme = true;
+            continue;
+        }
+        if (method < Basic && qStartsWithAuthenticationScheme(value, "basic")) {
             method = Basic;
-            headerVal = current.second.mid(6);
-        } else if (method < Ntlm && str.startsWith("ntlm")) {
+            headerVal = value.mid(6);
+        } else if (method < Ntlm && qStartsWithAuthenticationScheme(value, "ntlm")) {
             method = Ntlm;
-            headerVal = current.second.mid(5);
-        } else if (method < DigestMd5 && str.startsWith("digest")) {
+            headerVal = value.mid(5);
+        } else if (method < DigestMd5 && qStartsWithAuthenticationScheme(value, "digest")) {
             // Make sure the algorithm is actually MD5 before committing to it:
-            QByteArray fieldValue = current.second.mid(7);
+            QByteArray fieldValue = value.mid(7);
             if (!verifyDigestMD5(fieldValue))
                 continue;
 
             method = DigestMd5;
             headerVal = fieldValue;
-        } else if (method < Negotiate && str.startsWith("negotiate")) {
+        } else if (method < Negotiate && qStartsWithAuthenticationScheme(value, "negotiate")) {
 #if QT_CONFIG(sspi) || QT_CONFIG(gssapi) // if it's not supported then we shouldn't try to use it
 #if QT_CONFIG(gssapi)
             // For GSSAPI there needs to be a KDC set up for the host (afaict).
@@ -488,7 +514,7 @@ void QAuthenticatorPrivate::parseHttpResponse(const QList<QPair<QByteArray, QByt
                 continue;
 #endif
             method = Negotiate;
-            headerVal = current.second.mid(10);
+            headerVal = value.mid(10);
 #endif
         }
     }
@@ -532,7 +558,7 @@ void QAuthenticatorPrivate::parseHttpResponse(const QList<QPair<QByteArray, QByt
     default:
         realm.clear();
         challenge = QByteArray();
-        phase = Invalid;
+        phase = skippedUnsafeProxyAuthenticationScheme ? Done : Invalid;
     }
 }
 
@@ -1328,30 +1354,33 @@ static QByteArray clientChallenge(const QAuthenticatorPrivate *ctx)
     return clientCh;
 }
 
-// caller has to ensure a valid targetInfoBuff
 static QByteArray qExtractServerTime(const QByteArray& targetInfoBuff)
 {
-    QByteArray timeArray;
-    QDataStream ds(targetInfoBuff);
-    ds.setByteOrder(QDataStream::LittleEndian);
+    const uchar *p = reinterpret_cast<const uchar *>(targetInfoBuff.constData());
+    const uchar *end = p + targetInfoBuff.size();
 
-    quint16 avId;
-    quint16 avLen;
+    while (end - p >= 4) {
+        const quint16 avId = qFromLittleEndian<quint16>(p);
+        p += 2;
+        const quint16 avLen = qFromLittleEndian<quint16>(p);
+        p += 2;
 
-    ds >> avId;
-    ds >> avLen;
-    while(avId != 0) {
-        if(avId == AVTIMESTAMP) {
-            timeArray.resize(avLen);
-            //avLen size of QByteArray is allocated
-            ds.readRawData(timeArray.data(), avLen);
-            break;
+        if (avId == 0)
+            return QByteArray();
+
+        if (avLen > end - p)
+            return QByteArray();
+
+        if (avId == AVTIMESTAMP) {
+            if (avLen != 8)
+                return QByteArray();
+            return QByteArray(reinterpret_cast<const char *>(p), 8);
         }
-        ds.skipRawData(avLen);
-        ds >> avId;
-        ds >> avLen;
+
+        p += avLen;
     }
-    return timeArray;
+
+    return QByteArray();
 }
 
 static QByteArray qEncodeNtlmv2Response(const QAuthenticatorPrivate *ctx,
@@ -1382,8 +1411,8 @@ static QByteArray qEncodeNtlmv2Response(const QAuthenticatorPrivate *ctx,
     }
 
     //if server sends time, use it instead of current time
-    if(timeArray.size()) {
-        ds.writeRawData(timeArray.constData(), timeArray.size());
+    if(timeArray.size() == 8) {
+        ds.writeRawData(timeArray.constData(), 8);
     } else {
         // number of seconds between 1601 and the epoch (1970)
         // 369 years, 89 leap years
@@ -1440,6 +1469,12 @@ static QByteArray qEncodeLmv2Response(const QAuthenticatorPrivate *ctx,
     return lmChallengeResp;
 }
 
+static bool qNtlmBufferFits(const QNtlmBuffer &buffer, int dataSize)
+{
+    const quint32 size = quint32(dataSize);
+    return buffer.offset <= size && buffer.len <= size - buffer.offset;
+}
+
 static bool qNtlmDecodePhase2(const QByteArray& data, QNtlmPhase2Block& ch)
 {
     Q_ASSERT(QNtlmPhase2BlockBase::Size == sizeof(QNtlmPhase2BlockBase));
@@ -1466,14 +1501,14 @@ static bool qNtlmDecodePhase2(const QByteArray& data, QNtlmPhase2Block& ch)
     ds >> ch.targetInfo;
 
     if (ch.targetName.len > 0) {
-        if (ch.targetName.len + ch.targetName.offset > (unsigned)data.size())
+        if (!qNtlmBufferFits(ch.targetName, data.size()))
             return false;
 
         ch.targetNameStr = qStringFromUcs2Le(data.mid(ch.targetName.offset, ch.targetName.len));
     }
 
     if (ch.targetInfo.len > 0) {
-        if (ch.targetInfo.len + ch.targetInfo.offset > (unsigned)data.size())
+        if (!qNtlmBufferFits(ch.targetInfo, data.size()))
             return false;
 
         ch.targetInfoBuff = data.mid(ch.targetInfo.offset, ch.targetInfo.len);
diff --git a/src/network/kernel/qauthenticator_p.h b/src/network/kernel/qauthenticator_p.h
index b234925a0e7..f53898c20c3 100644
--- a/src/network/kernel/qauthenticator_p.h
+++ b/src/network/kernel/qauthenticator_p.h
@@ -114,7 +114,8 @@ public:
     QByteArray digestMd5Response(const QByteArray &challenge, const QByteArray &method, const QByteArray &path);
     static QHash<QByteArray, QByteArray> parseDigestAuthenticationChallenge(const QByteArray &challenge);
 
-    void parseHttpResponse(const QList<QPair<QByteArray, QByteArray> >&, bool isProxy, const QString &host);
+    void parseHttpResponse(const QList<QPair<QByteArray, QByteArray> >&, bool isProxy, const QString &host,
+                           bool allowUnsafeProxyMethods = false);
     void updateCredentials();
 };
 
diff --git a/src/network/kernel/qnetworkproxy.cpp b/src/network/kernel/qnetworkproxy.cpp
index 745e26ac959..7c7fd2914fe 100644
--- a/src/network/kernel/qnetworkproxy.cpp
+++ b/src/network/kernel/qnetworkproxy.cpp
@@ -373,6 +373,17 @@ QList<QNetworkProxy> QGlobalNetworkProxy::proxyForQuery(const QNetworkProxyQuery
         } else if (useSystemProxies) {
             result = QNetworkProxyFactory::systemProxyForQuery(query);
 
+            // Permit NTLM/Negotiate proxy authentication only for HTTP proxies
+            // that come from the system configuration. A proxy configured inside
+            // the application (or set via a tg://proxy link) is never trusted
+            // with it, because it could silently relay the user's credentials.
+            for (QNetworkProxy &proxy : result) {
+                if (proxy.type() == QNetworkProxy::HttpProxy
+                    || proxy.type() == QNetworkProxy::HttpCachingProxy) {
+                    proxy.setNtlmAuthenticationAllowed(true);
+                }
+            }
+
             // Make sure NoProxy is in the list, so that QTcpServer can work:
             // it searches for the first proxy that can has the ListeningCapability capability
             // if none have (as is the case with HTTP proxies), it fails to bind.
@@ -455,6 +466,10 @@ public:
     quint16 port;
     QNetworkProxy::ProxyType type;
     bool capabilitiesSet;
+    // Whether NTLM/Negotiate proxy authentication is permitted for this proxy.
+    // Only set for HTTP proxies that come from the system configuration; a
+    // proxy configured inside the application is never trusted with it.
+    bool ntlmAuthAllowed = false;
     QNetworkHeadersPrivate headers;
 
     inline QNetworkProxyPrivate(QNetworkProxy::ProxyType t = QNetworkProxy::DefaultProxy,
@@ -789,6 +804,33 @@ QNetworkProxy QNetworkProxy::applicationProxy()
     return QNetworkProxy();
 }
 
+/*!
+    \internal
+
+    Marks this proxy as permitted to use NTLM/Negotiate proxy authentication.
+    It is refused by default, because a malicious proxy can use it to silently
+    relay the user's Windows domain credentials. It is only set for HTTP proxies
+    that come from the system configuration; a proxy configured inside the
+    application is never trusted with it.
+*/
+void QNetworkProxy::setNtlmAuthenticationAllowed(bool allowed)
+{
+    d->ntlmAuthAllowed = allowed;
+}
+
+/*!
+    \internal
+
+    Returns whether NTLM/Negotiate proxy authentication is permitted for this
+    proxy.
+
+    \sa setNtlmAuthenticationAllowed()
+*/
+bool QNetworkProxy::isNtlmAuthenticationAllowed() const
+{
+    return d && d->ntlmAuthAllowed;
+}
+
 /*!
     \since 5.0
     Returns the value of the known network header \a header if it is
diff --git a/src/network/kernel/qnetworkproxy.h b/src/network/kernel/qnetworkproxy.h
index d267e744e37..23fd46a74e3 100644
--- a/src/network/kernel/qnetworkproxy.h
+++ b/src/network/kernel/qnetworkproxy.h
@@ -198,6 +198,12 @@ public:
     static void setApplicationProxy(const QNetworkProxy &proxy);
     static QNetworkProxy applicationProxy();
 
+    // \internal desktop-app patch: marks this proxy as permitted to use
+    // NTLM/Negotiate proxy authentication. Set only for HTTP proxies that come
+    // from the system configuration; never for application-configured proxies.
+    void setNtlmAuthenticationAllowed(bool allowed);
+    bool isNtlmAuthenticationAllowed() const;
+
     // "cooked" headers
     QVariant header(QNetworkRequest::KnownHeaders header) const;
     void setHeader(QNetworkRequest::KnownHeaders header, const QVariant &value);
diff --git a/src/network/socket/qhttpsocketengine.cpp b/src/network/socket/qhttpsocketengine.cpp
index 6629b7eaceb..7cb69ec3622 100644
--- a/src/network/socket/qhttpsocketengine.cpp
+++ b/src/network/socket/qhttpsocketengine.cpp
@@ -523,9 +523,16 @@ void QHttpSocketEngine::slotSocketConnected()
     QAuthenticatorPrivate *priv = QAuthenticatorPrivate::getPrivate(d->authenticator);
     //qDebug() << "slotSocketConnected: priv=" << priv << (priv ? (int)priv->method : -1);
     if (priv && priv->method != QAuthenticatorPrivate::None) {
-        d->credentialsSent = true;
-        data += "Proxy-Authorization: " + priv->calculateResponse(method, path, d->proxy.hostName());
-        data += "\r\n";
+        const bool unsafeProxyAuth = (priv->method == QAuthenticatorPrivate::Ntlm
+                || priv->method == QAuthenticatorPrivate::Negotiate)
+                && !d->proxy.isNtlmAuthenticationAllowed();
+        if (unsafeProxyAuth) {
+            d->authenticator = QAuthenticator();
+        } else {
+            d->credentialsSent = true;
+            data += "Proxy-Authorization: " + priv->calculateResponse(method, path, d->proxy.hostName());
+            data += "\r\n";
+        }
     }
     data += "\r\n";
 //     qDebug() << ">>>>>>>> sending request" << this;
@@ -610,7 +617,25 @@ void QHttpSocketEngine::slotSocketReadNotification()
             priv->hasFailed = true;
         }
 
-        priv->parseHttpResponse(d->reply->header(), true, d->proxy.hostName());
+        const bool allowUnsafeProxyAuth = d->proxy.isNtlmAuthenticationAllowed();
+        priv->parseHttpResponse(d->reply->header(), true, d->proxy.hostName(), allowUnsafeProxyAuth);
+
+        if (!allowUnsafeProxyAuth) {
+            // Refuse a proxy that requests NTLM/Negotiate, or one whose only
+            // offered schemes were the unsafe ones (skipped while parsing, so
+            // method stays None but the phase is marked Done).
+            if (priv->method == QAuthenticatorPrivate::Ntlm
+                    || priv->method == QAuthenticatorPrivate::Negotiate
+                    || (priv->method == QAuthenticatorPrivate::None
+                        && priv->phase == QAuthenticatorPrivate::Done)) {
+                d->authenticator = QAuthenticator();
+                setError(QAbstractSocket::ProxyAuthenticationRequiredError,
+                         tr("Proxy authentication method is not supported"));
+                d->socket->disconnectFromHost();
+                emitConnectionNotification();
+                return;
+            }
+        }
 
         if (priv->phase == QAuthenticatorPrivate::Invalid) {
             // problem parsing the reply
